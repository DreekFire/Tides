-- Settings
-- turn radius at maximum speed
local maxTurnRadius = 100

-- names of wheels
local wheelNames = {
  frontLeft="frontLeft",
  frontRight="frontRight",
  backLeft="backLeft",
  backRight="backRight",
}

function Update(I)
 -- turn radius = maxTurnRadius * forward/yaw
end